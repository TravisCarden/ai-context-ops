# Benchmarks

**Status:** scaffold. MVP (factual).

Real numbers and how to reproduce them (`rtk gain`, `headroom stats`,
`/caveman-stats`). No unverified claims.

<!-- TODO(mvp): drop in reproducible measurements. -->
